{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "rm -f bun.lockb && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}